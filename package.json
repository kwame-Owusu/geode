{
  "name": "geode",
  "version": "0.0.1",
  "description": "Remote sync, MCP, and an API for your Obsidian vault.",
  "private": true,
  "scripts": {
    "audit": "npm audit --omit=dev",
    "build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
    "check-versions": "node scripts/check-version-sync.mjs",
    "create-dev-vault": "node scripts/create-dev-vault.mjs",
    "dev": "concurrently -n build,s3 -c blue,yellow \"node esbuild.config.mjs\" \"npm run dev:s3\"",
    "dev:s3": "docker compose up",
    "dev:s3:reset": "docker compose down -v",
    "format": "biome check --write .",
    "lint": "biome check .",
    "test": "node --test \"src/**/*.test.ts\"",
    "test:integration": "docker compose up -d minio && docker compose up create-bucket && node --test \"src/**/*.itest.ts\""
  },
  "license": "GPL-3.0-only",
  "dependencies": {
    "aws4fetch": "1.x.x"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.3",
    "@types/node": "24.x.x",
    "concurrently": "10.x.x",
    "esbuild": "0.x.x",
    "obsidian": "1.x.x",
    "typescript": "7.x.x"
  }
}
